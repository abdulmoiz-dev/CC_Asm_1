fn int my_fn(int x, float y) {
    string my_str = "hmm";
    bool my_bool = x == 40;
    return x;
}

// This is a comment
int another_fn() {
    float pi = 3.14;
    string escaped = "Hello\nWorld\t\"Quoted\"";
    bool flag = true;
    
    if (x <= 50 && y >= 20) {
        return x * y + 10;
    }
    
    for (int i = 0; i < 10; i++) {
        cout << "Iteration: " << i << endl;
    }
    
    /* Multi-line this should eb ignored
       comment */
    int arr[5] = {1, 2, 3, 4, 5};
    
    return 0;
}